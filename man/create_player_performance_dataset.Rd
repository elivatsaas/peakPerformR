% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_player_performance_dataset.R
\name{create_player_performance_dataset}
\alias{create_player_performance_dataset}
\title{Create Player Performance Dataset}
\usage{
create_player_performance_dataset(
  sports_data,
  prime_data = NULL,
  cluster_data = NULL,
  prime_file = NULL,
  cluster_file = NULL,
  tier_mapping = c(Elite = 5, Great = 4, Average = 3, `Below Average` = 2,
    `Replacement Level` = 1)
)
}
\arguments{
\item{sports_data}{A data frame containing player sports data.
Must include 'id' and 'age' columns.}

\item{prime_data}{A data frame containing player prime years information.
Must include 'id', 'start_age', 'end_age', and 'max_value_age' columns.}

\item{cluster_data}{A data frame containing player performance clusters.
Must include 'id', 'age', 'performance_tier', 'scaled_value', and 'cluster' columns.}

\item{prime_file}{Character. Optional file path to load prime data from.}

\item{cluster_file}{Character. Optional file path to load cluster data from.}

\item{tier_mapping}{A named vector mapping performance tier names to numeric scores.
Default maps the standard five tiers to values 5-1.}
}
\value{
A data frame with combined player data including prime indicators and performance tiers.
}
\description{
Combines player sports data, prime years information, and performance clusters
into a comprehensive dataset for player career analysis.
}
\examples{
\dontrun{
# Load data from files
sports <- build_all_sports()
player_prime <- readr::read_csv("player_primes_pct.csv")
cluster_data <- readr::read_csv("season_clusters.csv")

# Combine the data
combined_data <- create_player_performance_dataset(
  sports_data = sports,
  prime_data = player_prime,
  cluster_data = cluster_data
)

# Alternatively, specify file paths directly
combined_data <- create_player_performance_dataset(
  sports_data = sports,
  prime_file = "player_primes_pct_3min_8max_cubic.csv",
  cluster_file = "season_clusters_3min_8max_cubic.csv"
)
}

}
